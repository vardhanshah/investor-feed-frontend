# API Configuration
# Backend API base URL
# IMPORTANT: Use localhost instead of 0.0.0.0 for local development
# Browsers don't send cookies from localhost to 0.0.0.0
VITE_API_BASE_URL=http://localhost:8000
